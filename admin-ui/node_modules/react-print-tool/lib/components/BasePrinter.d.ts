import React from "react";
export interface IBasePrinterProps {
    unMount: () => void;
}
export declare abstract class BasePrinter<T extends IBasePrinterProps> extends React.Component<T> {
    constructor(props: T);
    private _styleTag;
    protected abstract _renderContent(): JSX.Element;
    render(): JSX.Element;
    componentDidCatch(): void;
    componentDidMount(): void;
}
